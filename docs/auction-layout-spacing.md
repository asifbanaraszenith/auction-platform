# Auction layout spacing

All auction editor configuration sections use the same section spacing. Structural selectors such as `:first-of-type` must not remove spacing from Categories or later sections.